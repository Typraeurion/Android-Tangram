<?xml version="1.0" encoding="utf-8"?>
<!--
  ~ Circular semi-opaque backing so the control text stays legible
  ~ over any image.
  -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="@dimen/hover_text_background_radius" />
    <solid android:color="@color/shadow_background" />
</shape>
